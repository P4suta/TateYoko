# Normalize line endings: LF in the repository, native on checkout (CRLF on
# Windows, matching .editorconfig). Explicit binaries are never normalized.
* text=auto

*.png binary
*.ico binary
*.pdf binary
*.zip binary
*.snk binary
